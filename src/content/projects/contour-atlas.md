---
title: "Contour Atlas"
tagline: "Turns any scalar field into a printable topographic map."
status: shipped
order: 1
stack: ["typescript", "svg", "node"]
repo: "https://github.com/eltabre/placeholder-contour-atlas"
demo: "https://example.com/placeholder-demo"
blogPost: "https://example.com/placeholder-writeup"
---

Placeholder content. This entry has **every optional field filled in**, so the
project dialog should show links to the repo, the demo, and the writeup.

Samples a function onto a grid, traces level sets with marching squares, and
simplifies the resulting polylines before writing SVG. The same code path takes
elevation rasters and loss surfaces, because they are the same kind of object.
