---
title: "Frame Differ"
tagline: "Finds the handful of frames in an hour of footage where something changed."
status: building
order: 2
stack: ["python", "opencv", "numpy"]
---

Placeholder content. This entry has **no optional fields at all** — no repo, no
demo, no blog post. The dialog should render no links section whatsoever, rather
than empty or dead buttons.

It is also `status: building`, so its trail blaze should render open rather than
filled.
