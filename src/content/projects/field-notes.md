---
title: "Field Notes"
tagline: "A deliberately long tagline, written this way to check what happens to a project tile when the one-line description refuses to stay on one line."
status: building
order: 4
stack: ["astro", "typescript", "sqlite", "leaflet", "gdal"]
blogPost: "https://example.com/placeholder-field-notes"
---

Placeholder content. Two things to look at here: the tagline is long enough to
wrap onto several lines, and the stack has five entries rather than three. Both
are the cases where a grid tile usually breaks.

Its `order` is 4 but its filename sorts second alphabetically, so it also proves
the grid is sorting on `order` and not on filename.
